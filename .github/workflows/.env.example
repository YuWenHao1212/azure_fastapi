# GitHub Actions Secrets Required
# Copy this file to your GitHub repository settings -> Secrets and variables -> Actions

## Deployment Secrets (azure-deploy.yml)

# Azure Service Principal credentials for deployment
# Format: JSON object with clientId, clientSecret, subscriptionId, tenantId
AZURE_CREDENTIALS=

# Function App host key for health check endpoint
# Get from: Azure Portal -> Function App -> App keys -> Host keys
FUNCTION_KEY=

## Consistency Monitoring Secrets (consistency-monitoring-fixed.yml) - Currently Disabled

# OpenAI API key for accessing the language model
OPENAI_API_KEY=

# Base URL for OpenAI API endpoint
# Example: https://your-resource.openai.azure.com/
OPENAI_API_BASE=

# API version for OpenAI service
# Example: 2023-05-15
OPENAI_API_VERSION=

# Deployment name for OpenAI model
OPENAI_DEPLOYMENT_NAME=

# Optional: Slack webhook for notifications when KPIs fail
# SLACK_WEBHOOK_URL=

## Optional Environment Variables

# Optional: Azure Resource Group (if not using default)
# AZURE_RESOURCE_GROUP=airesumeadvisorfastapi

# Optional: Function App Name (if not using default)
# AZURE_FUNCTIONAPP_NAME=airesumeadvisorfastapi